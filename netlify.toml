[build]
  command = "pnpm build && pnpm sanity deploy -y"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
